import React from 'react'
import PropTypes from 'prop-types'

const Projects = (props) => {
    return (
      <React.Fragment>
        <section className={'projects'} data-sal={'fade'} data-sal-delay={300} data-sal-easing={'ease'}>
         
        </section>
      </React.Fragment>
    );
};

export default Projects;