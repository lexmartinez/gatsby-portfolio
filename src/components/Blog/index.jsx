import React from 'react'
import PropTypes from 'prop-types'

const Blog = (props) => {
    return (
      <React.Fragment>
        <section className={'blog'}>
         
        </section>
      </React.Fragment>
    );
};

export default Blog;